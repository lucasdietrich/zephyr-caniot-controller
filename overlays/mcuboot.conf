CONFIG_BOOTLOADER_MCUBOOT=y

# Relative path to west workspace
CONFIG_MCUBOOT_SIGNATURE_KEY_FILE="zephyr-caniot-controller/creds/mcuboot/root-ec-p256.pem"

# Major, minor, revision, and build version numbers
# 0.0.0.0 is for development only
CONFIG_MCUBOOT_EXTRA_IMGTOOL_ARGS="--pad --version 0.0.0+0"
CONFIG_MCUBOOT_GENERATE_CONFIRMED_IMAGE=n

CONFIG_IMG_MANAGER=y
CONFIG_MCUBOOT_IMG_MANAGER=y
CONFIG_IMG_BLOCK_BUF_SIZE=512
CONFIG_IMG_ENABLE_IMAGE_CHECK=y
CONFIG_IMG_ERASE_PROGRESSIVELY=y